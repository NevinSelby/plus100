# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/latest/ before writing any code. The project tracks the LATEST Expo SDK (Expo Go drops old SDKs when it updates); check mobile/package.json for the current pin.
